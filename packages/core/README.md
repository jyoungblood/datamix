# `@datamix/core`

Shared Datamix domain types and cross-surface helpers.

This package should stay small until real reuse appears. It exists to keep the app boundaries honest, not to preemptively centralize everything.
