# `create-datamix`

Secondary bootstrap path for Datamix.

This package scaffolds a local Datamix workspace from the current project template and can print deploy-oriented next steps with `--deploy`.

It is intentionally a secondary path. The browser-first Cloudflare deploy flow remains the primary v0 onboarding experience.
