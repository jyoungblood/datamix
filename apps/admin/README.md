# `@datamix/admin`

Minimal Vinext admin scaffold for Datamix.

This app stays intentionally small in `M0-S3`: one page, one global stylesheet, one typed public env helper, and one shared import from `@datamix/core`.
