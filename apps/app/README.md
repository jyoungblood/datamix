# `@datamix/app`

Vinext App Router scaffold for the unified Datamix app.

This workspace is the migration target for the vinext-only architecture. It currently
preserves the existing admin UI through App Router pages while the old Hono API remains
available as the behavioral reference.

Backend routes move here in later migration sessions.
